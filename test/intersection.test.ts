import { Person } from "../src/intersection";

describe("intersection", () => {
	it("should be intersection", () => {
		const person: Person = {
			name: "Muhammad",
			age: 20,
		};

		console.log(person);
	});
});
